---
import type { CommentairesResponse, UsersResponse } from '../pocketbase-types'
import ProgressBar from './Progress_bar.astro';

type CommentaireWithUser = CommentairesResponse & {
    expand?: {
        user?: UsersResponse;
    };
};

interface Props {
    com: CommentaireWithUser;
}
const { com } = Astro.props;
---
<div class="bg-vert border border-beige w-9/10 mx-auto rounded-xl p-3.5">
    {/* espace du haut */}
    <div class="flex justify-around">
        {/* Nom & statut */}
        <div class="flex-2">
            
            {
                com.expand?.user?.coach ? (
                <a href={`/coachs/${com.expand?.user?.id}`} class="text-xl text-bleu">{com.expand?.user?.name}</a>
                <p class="text-sm">coach</p>
                ):(
                <p class="text-xl text-bleu">{com.expand?.user?.name}</p>
                <p class="text-sm">membre</p>
                )
            }
        </div>
        <div class="flex flex-1 place-items-baseline gap-2">
            <ProgressBar note={com.note} color='vert' />
            <p>{com.note}/5</p>
        </div>
    </div>
    <p class="text-sm mt-5">{com.contenu}</p>
</div>